import { Dimensions } from './types';

export const settings = {
    dimension: Dimensions.Two,
    iterations: {
        visible: true
    },
    fractal: null,
}

export const mouse = {
    x: 0,
    y: 0
}